<core:FragmentDefinition
  xmlns="sap.m"
  xmlns:core="sap.ui.core"
  xmlns:f="sap.ui.layout.form">
<Dialog title="Novo Produto">
        <content>
            <VBox>
              <f:SimpleForm>
                <f:content>
                  <Label text="ID" required="true"/>
                  <Input id="createId" value="{createProduct>/ID}" placeholder="Preencha" type="Number"/>
                  <Label text="Name" required="true"/>
                  <Input id="createName" value="{createProduct>/Name}" maxLength="120" placeholder="Preencha"/>
                  <Label text="Description"/>
                  <TextArea id="createDesc" value="{createProduct>/Description}" rows="4" growing="true" growingMaxLines="8" placeholder="Preencha"/>
                  <Label text="Rating"/>
                  <RatingIndicator id="createRating" value="{createProduct>/Rating}" maxValue="5" visualMode="Half" tooltip="0–5"/>
                  <Label text="Price" required="true"/>
                  <Input id="createPrice" value="{createProduct>/Price}" type="Number" placeholder="Preencha" />
                </f:content>
              </f:SimpleForm>
            </VBox>
        </content>
        <buttons>
          <Button text="Fechar" type="Transparent" press=".onCloseDialog"/>
          <Button text="Criar" type="Emphasized" press=".onCreateProduct"/>
        </buttons>
    </Dialog>
</core:FragmentDefinition>